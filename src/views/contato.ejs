<%- include('includes/head') %>
  <%- include('includes/nav') %>
    <div class="container">
      <div class="row">
        <div class="col-log-2"></div>

        <div class="col-log-8 my-3">
          <h1 class="text-center">Contato</h1>
          <p class="text-center lead"> Crie ou edite o seu contato abaixo</p>

          <%- include('includes/messages')%>
            <% if(contato.id) { %>
            <form action="/contato/edit/<%= contato.id %>" method="POST">

            <% } else { %>
               <form action="/contato/register" method="POST">
            <% } %>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <div class="form-group">
                <label>Nome</label>
                <input value="<%= contato.nome %>" type="text" class="form-control my-2" name="nome">
              </div>
              <div class="form-group">
                <label>Sobrenome</label>
                <input value="<%= contato.sobrenome %>" type="text" class="form-control my-2" name="sobrenome">
              </div>
              <div class="form-group">
                <label>Endereço de E-mail</label>
                <input value="<%= contato.email %>" type="email" class="form-control my-2" name="email">
              </div>
              <div class="form-group">
                <label>Telefone</label>
                <input value="<%= contato.telefone %>" type="tel" class="form-control my-2" name="telefone">
              </div>

              <button type="submit" class="btn btn-primary my-2">Salvar</button>
            </form>

        </div>
        <div class="col-log-2"></div>
      </div>

    </div>
    <%- include('includes/footer') %>